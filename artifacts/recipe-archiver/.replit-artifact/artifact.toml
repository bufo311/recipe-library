kind = "web"
previewPath = "/"
title = "Recipe Archiver"
version = "1.0.0"
id = "artifacts/recipe-archiver"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20739

[services.development]
run = "pnpm --filter @workspace/recipe-archiver run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/recipe-archiver", "run", "build" ]
publicDir = "artifacts/recipe-archiver/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20739"
BASE_PATH = "/"
